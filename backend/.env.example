# 將此檔複製成 .env 後填入實際值。本地開發若不需要 PostgreSQL，DATABASE_URL 留空即可，
# 系統會自動退回使用 backend/portfolio.db (SQLite)。

# Supabase / Render / Railway 等的 PostgreSQL 連線字串
# 範例：postgresql://postgres:YOUR_PASSWORD@db.xxxxxxxx.supabase.co:5432/postgres
DATABASE_URL=

# JWT 簽章密鑰（雲端部署時必須改成複雜亂數）
PORTFOLIO_SECRET=dev-secret-change-me
